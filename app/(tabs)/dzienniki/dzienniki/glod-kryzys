import { Text, View } from "react-native";

export default function DziennikGlodu() {
  return (
    <View style={{ padding: 16 }}>
      <Text style={{ fontSize: 18, fontWeight: "700" }}>Dziennik głodu / kryzysu</Text>
      <Text style={{ marginTop: 8, opacity: 0.7 }}>
        Tu będzie: objawy + Historia (wewnątrz dziennika)
      </Text>
    </View>
  );
}